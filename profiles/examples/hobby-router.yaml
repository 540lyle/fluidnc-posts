id: hobby-router-example
controller: fluidnc
defaults:
  spindleWarmupDelaySec: 3
  arcToleranceMm: 0.002
  minimumSegmentLengthMm: 0.05
  safeRetractMethod: G53
  coolantMode: none
notes:
  - Example only. Copy into `profiles/local/` and adjust for real hardware.
